#include "board.h"

void SystemInit(void) {
    // System initialization code can be added here
}

int main(void) {
    // Initialize the board
    Board_Init();

    // Main application loop
    while (1) {
        // Application code goes here
    }

    return 0; // This line will never be reached
}